import { GlobalState } from './types';

const GLOBAL_STATE: GlobalState = {
    carMove: [],
    createColor: '',
    createName: '',
    updateName: '',
    updateColor: '',
    isUpdate: false,
};

export default GLOBAL_STATE;
